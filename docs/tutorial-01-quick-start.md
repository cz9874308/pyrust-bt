# 第 1 课：5 分钟快速上手 - 运行你的第一个回测

## 简介

欢迎来到 pyrust-bt 回测框架！在这第一课中，我们将用最简单的方式理解什么是回测，并快速运行你的第一个回测示例。就像学开车一样，我们先上路跑一圈，感受一下，然后再深入学习细节。

**预计学习时间**：15-20 分钟

## 学习目标

完成本课后，你将能够：

- ✅ 理解回测的基本概念
- ✅ 完成环境安装和配置
- ✅ 运行第一个回测示例
- ✅ 查看和理解回测结果

## 前置知识

- Python 基础（变量、函数、类的基本概念）
- 命令行基础操作（cd、ls 等）
- 基本的文件操作

---

## 1. 什么是回测？（简单理解）

### 简单理解

想象一下，你有一个交易策略："当股价突破 5 日均线时买入，跌破时卖出"。这个策略听起来不错，但真的能赚钱吗？

**回测就是"时间机器"**：它让你回到过去，用历史数据测试你的策略，看看如果按照这个策略交易，能赚多少钱、会亏多少钱。

就像：
- 🎬 **看电影**：你可以快进、回放，看任何片段
- 📊 **回测**：你可以回到任何历史时间点，测试你的策略

### 为什么需要回测？

1. **验证策略有效性**：策略在历史数据上表现如何？
2. **评估风险**：最大亏损是多少？波动大不大？
3. **优化参数**：5 日均线好还是 10 日均线好？
4. **避免实盘损失**：先在历史数据上测试，再考虑实盘交易

### pyrust-bt 是什么？

pyrust-bt 是一个**高性能回测框架**，它结合了：
- **Python**：编写策略简单易懂（就像写普通 Python 代码）
- **Rust**：执行速度超快（比纯 Python 快 250 倍）

就像：
- 🐍 **Python**：你负责想策略（简单）
- ⚡ **Rust**：引擎负责快速执行（高效）

---

## 2. 环境安装

在开始之前，我们需要安装必要的工具。就像做菜需要准备食材一样，我们需要准备开发环境。

### 2.1 检查 Python 版本

首先，确保你的 Python 版本 >= 3.8：

```bash
python --version
# 或者
python3 --version
```

应该显示类似：`Python 3.8.x` 或更高版本。

### 2.2 安装 Rust

pyrust-bt 的核心引擎是用 Rust 编写的，所以需要安装 Rust：

**macOS/Linux:**
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

**Windows:**
访问 [https://rustup.rs/](https://rustup.rs/) 下载安装程序。

安装完成后，重启终端，验证安装：
```bash
rustc --version
```

### 2.3 安装 maturin

maturin 是用来构建 Python 扩展的工具：

```bash
pip install maturin
```

或者使用 conda：
```bash
conda install -c conda-forge maturin
```

### 2.4 编译 Rust 引擎

这是最关键的一步！我们需要将 Rust 代码编译成 Python 可以调用的模块：

```bash
# 进入 Rust 引擎目录
cd rust/engine_rust

# 编译并安装到当前 Python 环境
maturin develop --release
```

**注意**：
- `--release` 表示使用优化编译，速度更快（但编译时间稍长）
- 第一次编译可能需要 5-10 分钟，请耐心等待
- 如果遇到网络问题，可能需要配置 Rust 国内镜像源

编译成功后，你应该看到类似输出：
```
✅ Compiled successfully!
```

### 2.5 验证安装

让我们验证一下安装是否成功：

```python
python -c "from engine_rust import BacktestEngine, BacktestConfig; print('安装成功！')"
```

如果看到"安装成功！"，说明一切就绪！

---

## 3. 准备数据文件

回测需要历史数据。就像做实验需要数据一样，我们需要准备 K 线数据。

### 3.1 数据格式要求

数据文件必须是 CSV 格式，包含以下列：

```
datetime,open,high,low,close,volume
2020-01-02,100,101,99,100.5,10000
2020-01-03,100.5,101.5,100,101.0,12000
...
```

**字段说明**：
- `datetime`：时间（格式：YYYY-MM-DD 或 YYYY-MM-DD HH:MM:SS）
- `open`：开盘价
- `high`：最高价
- `low`：最低价
- `close`：收盘价
- `volume`：成交量

### 3.2 使用示例数据

项目已经提供了示例数据文件：`examples/data/sample.csv`

你可以直接使用它，或者准备自己的数据文件。

---

## 4. 第一个回测示例

现在，让我们运行第一个回测！这个示例非常简单，但包含了回测的所有核心步骤。

### 4.1 创建测试文件

创建一个新文件 `my_first_backtest.py`：

```python
from pyrust_bt.api import BacktestEngine, BacktestConfig
from pyrust_bt.strategy import Strategy
from pyrust_bt.data import load_csv_to_bars

# 1. 定义一个简单的策略
class SimpleStrategy(Strategy):
    """最简单的策略：价格高于 100 就买入，低于 100 就卖出"""
    
    def next(self, bar):
        """每根 K 线调用一次，返回交易信号"""
        close_price = bar["close"]
        
        if close_price > 100:
            return "BUY"  # 买入信号
        elif close_price < 100:
            return "SELL"  # 卖出信号
        return None  # 不交易

# 2. 配置回测参数
config = BacktestConfig(
    start="2020-01-01",      # 回测开始时间
    end="2020-12-31",        # 回测结束时间
    cash=10000.0,            # 初始资金：1 万元
    commission_rate=0.0005,   # 手续费率：0.05%（万五）
    slippage_bps=2.0,        # 滑点：2 个基点（0.02%）
)

# 3. 创建回测引擎
engine = BacktestEngine(config)

# 4. 加载数据
bars = load_csv_to_bars("examples/data/sample.csv", symbol="SAMPLE")

# 5. 运行回测
strategy = SimpleStrategy()
result = engine.run(strategy, bars)

# 6. 查看结果
print("\n" + "=" * 60)
print("回测结果")
print("=" * 60)
print(f"初始资金: {config.cash:,.2f} 元")
print(f"最终资金: {result['equity']:,.2f} 元")
print(f"总收益: {result['equity'] - config.cash:,.2f} 元")
print(f"收益率: {(result['equity'] / config.cash - 1) * 100:.2f}%")
print("=" * 60)
```

### 4.2 运行回测

在项目根目录下运行：

```bash
python my_first_backtest.py
```

### 4.3 预期输出

你应该看到类似这样的输出：

```
============================================================
回测结果
============================================================
初始资金: 10,000.00 元
最终资金: 10,050.00 元
总收益: 50.00 元
收益率: 0.50%
============================================================
```

**恭喜！** 你已经成功运行了第一个回测！🎉

---

## 5. 理解回测结果

让我们看看回测结果都包含什么信息：

### 5.1 基本结果字段

```python
result = {
    'cash': 9950.0,           # 当前现金
    'position': 1.0,          # 当前持仓（股数）
    'avg_cost': 100.5,        # 平均成本
    'equity': 10050.0,        # 总资产（现金 + 持仓市值）
    'realized_pnl': 0.0,      # 已实现盈亏
    'stats': {...},           # 详细统计指标
    'equity_curve': [...],    # 净值曲线
    'trades': [...]           # 交易记录
}
```

### 5.2 统计指标

`result['stats']` 包含更详细的统计信息：

```python
stats = result['stats']
print(f"总收益率: {stats['total_return']:.2%}")
print(f"年化收益率: {stats['annualized_return']:.2%}")
print(f"夏普比率: {stats['sharpe']:.4f}")
print(f"最大回撤: {stats['max_drawdown']:.2%}")
print(f"交易次数: {stats['total_trades']}")
```

这些指标帮助我们评估策略的表现，我们会在第 5 课详细学习。

---

## 6. 代码解析（简单理解）

让我们看看刚才的代码做了什么：

### 6.1 策略类（SimpleStrategy）

```python
class SimpleStrategy(Strategy):
    def next(self, bar):
        # bar 是当前 K 线数据，包含 open, high, low, close, volume
        close_price = bar["close"]
        
        if close_price > 100:
            return "BUY"  # 返回买入信号
        elif close_price < 100:
            return "SELL"  # 返回卖出信号
        return None  # 不交易
```

**简单理解**：
- `next()` 方法就像"决策函数"
- 每根 K 线，引擎都会问策略："现在该买还是卖？"
- 策略根据当前价格决定："买"、"卖"或"不动"

### 6.2 回测配置（BacktestConfig）

```python
config = BacktestConfig(
    start="2020-01-01",      # 从什么时候开始
    end="2020-12-31",        # 到什么时候结束
    cash=10000.0,            # 初始有多少钱
    commission_rate=0.0005,  # 每次交易收多少手续费
    slippage_bps=2.0,        # 交易时的滑点（价格偏差）
)
```

**简单理解**：
- 就像设置"游戏规则"
- 告诉引擎："用 1 万元，从 2020 年 1 月 1 日到 12 月 31 日，测试这个策略"

### 6.3 运行回测

```python
engine = BacktestEngine(config)  # 创建引擎
bars = load_csv_to_bars(...)     # 加载数据
result = engine.run(strategy, bars)  # 运行回测
```

**简单理解**：
- 创建引擎 → 加载数据 → 运行回测 → 得到结果
- 就像：启动机器 → 放入材料 → 按下按钮 → 得到产品

---

## 7. 常见问题

### Q1: 编译 Rust 引擎失败？

**可能原因**：
- Rust 未正确安装
- 网络问题（需要下载依赖）
- 权限问题

**解决方法**：
- 检查 Rust 安装：`rustc --version`
- 配置 Rust 国内镜像源
- 使用 `sudo`（Linux/Mac）或管理员权限（Windows）

### Q2: 找不到数据文件？

**解决方法**：
- 检查文件路径是否正确
- 确保 CSV 文件格式正确（包含表头）
- 使用绝对路径或相对路径

### Q3: 导入模块失败？

**错误信息**：`ModuleNotFoundError: No module named 'engine_rust'`

**解决方法**：
- 确保已经编译 Rust 引擎：`cd rust/engine_rust && maturin develop --release`
- 检查是否在正确的 Python 环境中
- 尝试重新编译

### Q4: 回测结果异常？

**可能原因**：
- 数据格式不正确
- 策略逻辑有误
- 配置参数不合理

**解决方法**：
- 检查数据文件格式
- 打印中间结果调试
- 查看错误日志

---

## 本节要点

✅ **回测就是"时间机器"**：用历史数据测试策略  
✅ **环境安装三步走**：Python → Rust → maturin → 编译引擎  
✅ **回测四步走**：定义策略 → 配置参数 → 加载数据 → 运行回测  
✅ **策略就是实现 `next()` 方法**：根据当前 K 线返回交易信号  
✅ **结果包含账户信息和统计指标**：帮助我们评估策略表现

---

## 下一步学习

恭喜你完成了第一课！🎉

现在你已经：
- ✅ 理解了回测的基本概念
- ✅ 成功运行了第一个回测
- ✅ 看到了回测结果

**下一步**：继续学习 [第 2 课：理解核心概念](./tutorial-02-core-concepts.md)，深入了解回测框架的核心组件和工作原理。

---

## 扩展阅读

- 查看项目 README：[../README.md](../README.md)
- 查看示例代码：[../examples/run_mvp.py](../examples/run_mvp.py)
- 了解更多策略示例：[../examples/](../examples/)

---

**记住**：回测不是预测未来，而是验证策略在历史数据上的表现。好的回测结果不代表未来一定赚钱，但可以帮助我们理解策略的风险和收益特征。

**继续加油！** 🚀

